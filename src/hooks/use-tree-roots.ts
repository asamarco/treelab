/**
 * @fileoverview
 * This module exports the useTreeRoots hook from its new location in the tree-roots context file.
 * This separation is necessary because the hook contains JSX, which requires a .tsx file extension.
 */
export { useTreeRoots } from '@/contexts/tree-roots';
